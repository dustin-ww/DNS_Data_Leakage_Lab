- name: Ensure categories directory exists
  file:
    path: /opt/dns/data/categories
    state: directory
    owner: stubby
    group: stubby
    mode: '0755'
    recurse: yes

- name: Upload category CSV files
  copy:
    src: "{{ item }}"
    dest: "/opt/dns/data/categories/"
    owner: stubby
    group: stubby
    mode: '0644'
  loop:
    - "{{ data_source_directory }}/news.csv"
    - "{{ data_source_directory }}/banking.csv"
    - "{{ data_source_directory }}/streaming.csv"