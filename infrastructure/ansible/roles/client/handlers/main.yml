---
# handlers/main.yml

- name: Reload systemd
  systemd:
    daemon_reload: yes

- name: Restart stubby
  systemd:
    name: stubby
    state: restarted

- name: Restart systemd-resolved
  systemd:
    name: systemd-resolved
    state: restarted
